{% if customer_group_id==retailer_id %}

{{ header }}  
<div id="account-order" class="container">
  <ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
    {% endfor %}
  </ul>
  <div class="row">{{ column_left }}
    {% if column_left and column_right %}
    {% set class = 'col-sm-6' %}
    {% elseif column_left or column_right %}
    {% set class = 'col-sm-9' %}
    {% else %}
    {% set class = 'col-sm-12' %}
    {% endif %}
    <div id="content" class="{{ class }}">{{ content_top }}
      <h1>{{ heading_title }} {{ customer_group_id }}</h1>
      {% if orders %}
      <div class="table-responsive">
        <table class="table table-bordered table-hover">
          <thead>
            <tr>
          <th>order id</th>
            <th>Store name</th>
            <th>Customer Name</th>
             <th>Customer Address</th>
              <th>Customer Telephone</th>
               <th>Customer Email</th>
               <th>Status</th>
        </tr>
          </thead>
          <tbody>
           {% for order in orders %}
            <tr>
              <td class="text-right">{{ order.order_id }}</td>
              <td class="text-right">{{ order.store_name }}</td>
              <td class="text-left">{{ order.firstname }}{{ order.lastname }}</td>
              <td class="text-left">{{ order.payment_address_1 }}</td>
              <td class="text-right">{{ order.customer_telephone }}</td>
              <td class="text-left">{{ order.customer_email }}</td>
              <td class="text-right"><a href="{{ order.detailsinfo }}" data-toggle="tooltip" title="{{ button_view }}" class="btn btn-info"><i class="fa fa-eye"></i></a></td>
              
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
 
      {% else %}
      <p>{{ text_empty }}</p>
      {% endif %}
      <div class="buttons clearfix">
        
      </div>
      {{ content_bottom }}</div>
    {{ column_right }}</div>
</div>
{{ footer }}



{% endif %}

{% if customer_group_id == distributer_id%}
{{ header }}
<div id="account-order" class="container">
  <ul class="breadcrumb">
    {% for breadcrumb in breadcrumbs %}
    <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
    {% endfor %}
  </ul>
  <div class="row">{{ column_left }}
    {% if column_left and column_right %}
    {% set class = 'col-sm-6' %}
    {% elseif column_left or column_right %}
    {% set class = 'col-sm-9' %}
    {% else %}
    {% set class = 'col-sm-12' %}
    {% endif %}
    <div id="content" class="{{ class }}">{{ content_top }}
      <h1>{{ heading_title }} {{ customer_group_id }}</h1>
      {% if orders %}
      <div class="table-responsive">
        <table class="table table-bordered table-hover">
          <thead>
            <tr>
         <th>order date</th>
          <th>order id</th>
            
            <th>Store name</th>
             <th>Address</th>
              <th>Telephone</th>
               <th>Email</th>
               <th>Estimate Delivery date</th>
               <th>Status</th>

        </tr>
          </thead>
          <tbody>
           {% for order in orders %}
            <tr>
             <td class="text-right">{{ order.order_date }}</td>
              <td class="text-right">{{ order.order_id }}</td>
              
              <td class="text-right">{{ order.shop_name }}</td>
              <td class="text-left">{{ order.payment_address_1 }}</td>
              <td class="text-right">{{ order.customer_telephone }}</td>
              <td class="text-left">{{ order.customer_email }}</td>
              <td class="text-left">{{ estimate_date }}</td>
              <td class="text-right"><a href="{{ order.detailsinfo }}" data-toggle="tooltip" title="{{ button_view }}" class="btn btn-info"><i class="fa fa-eye"></i></a></td>
              
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
 
      {% else %}
      <p>{{ text_empty }}</p>
      {% endif %}
      <div class="buttons clearfix">
        
      </div>
      {{ content_bottom }}</div>
    {{ column_right }}</div>
</div>
{{ footer }}



{% endif %}